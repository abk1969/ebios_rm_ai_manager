/**
 * 🤖 PAGE DU GÉNÉRATEUR AUTOMATIQUE DE MISSIONS
 * Interface principale pour la génération automatique de missions EBIOS RM
 */

import React from 'react';
import ModernAutoMissionGenerator from '@/components/mission-generator/ModernAutoMissionGenerator';

const AutoMissionGeneratorPage: React.FC = () => {
  return <ModernAutoMissionGenerator />;
};

export default AutoMissionGeneratorPage;
