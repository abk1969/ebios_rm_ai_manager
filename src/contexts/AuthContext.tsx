/**
 * 🚨 FICHIER OBSOLÈTE - NE PAS UTILISER
 * 
 * Ce fichier a été remplacé par le nouveau système d'authentification.
 * Utilisez @/hooks/useAuth à la place.
 * 
 * Ce fichier existe uniquement pour éviter les erreurs HMR.
 */

// Export vide pour éviter les erreurs d'import
export const AuthContext = null;
export const useAuth = () => {
  throw new Error('useAuth obsolète - utilisez @/hooks/useAuth');
};
export default null;
